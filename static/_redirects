# Netlify redirects file

# Redirect 404s to index.html (404 should be handled by client)
/* /index.html 200

# Redirect default Netlify subdomain to primary domain
https://hyperstatic-demo.netlify.com/* https://hyperstatic.dev/:splat 301!

